import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "KRITAM OS — Hire AI Employees in Minutes",
  description:
    "KRITAM OS is the autonomous AI workforce platform. Deploy AI employees across Sales, HR, Finance and Engineering in minutes. Scale your business without scaling your team.",
  keywords: ["KRITAM OS", "AI Employees", "Autonomous Agents", "AI Workforce", "Z.ai"],
  authors: [{ name: "Kritam Space" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
